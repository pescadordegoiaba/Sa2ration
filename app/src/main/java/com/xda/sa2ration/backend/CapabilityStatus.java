package com.xda.sa2ration.backend;

public enum CapabilityStatus {
    SUPPORTED,
    UNSUPPORTED,
    EXPERIMENTAL,
    UNTESTED,
    PREPARED,
    REQUIRES_MODULE,
    REQUIRES_ADAPTER,
    FAILED,
    UNKNOWN
}
